# Backlog

## Open items

- [ ] Consider renaming the GitHub repo and the Vercel project from `math-flap2` to `skysum`, to match the brand.
- [ ] Optional: replace the owl's wing flap with 4 hand-drawn frames. The flap works now by slicing the wings out of math-owl.png and rotating them (`drawWing` in `drawBird()`, public/game.html). Real frames would look smoother, but they're only worth doing if the frames get made.
- [ ] Optional: a dedicated app-icon drawing (just the owl's face) would read better on home screens than the wide flying-owl sprite. The icon is drawn in `app/_lib/app-icon.tsx`.
- [ ] Decide whether to clear the test players and scores created during setup (2026-09-17) from the production database.
